apiVersion: apps/v1
kind: Deployment
metadata:
  name: gaea-retriever
  namespace: gaea
spec:
  replicas: 2
  selector:
    matchLabels:
      app: gaea-retriever
  template:
    metadata:
      labels:
        app: gaea-retriever
    spec:
      serviceAccountName: gaea-service-account
      containers:
      - name: retriever
        image: us-central1-docker.pkg.dev/PROJECT_ID/gaea/retriever:latest
        ports:
        - containerPort: 8080
        env:
        - name: GAEA_ENV
          value: "prod"
        - name: SERVICE_NAME
          value: "retriever"
        envFrom:
        - secretRef:
            name: gaea-secrets
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: gaea-retriever-service
  namespace: gaea
spec:
  selector:
    app: gaea-retriever
  ports:
  - port: 80
    targetPort: 8080
  type: ClusterIP